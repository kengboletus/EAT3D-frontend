import React from "react";
import { StyleSheet, Text, View } from "react-native";

const id = () => {
  return (
    <View>
      <Text>id</Text>
    </View>
  );
};

export default id;

const styles = StyleSheet.create({});